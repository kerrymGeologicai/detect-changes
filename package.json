{
  "name": "detect-changes",
  "version": "1.0.0",
  "description": "a reusable github workflow to detect and list changes on push and PR events",
  "main": "index.js",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
